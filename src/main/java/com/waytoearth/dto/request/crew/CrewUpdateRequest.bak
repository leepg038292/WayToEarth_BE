package com.waytoearth.dto.request.crew;

import io.swagger.v3.oas.annotations.media.Schema;
import jakarta.validation.constraints.Max;
import jakarta.validation.constraints.Min;
import jakarta.validation.constraints.Pattern;
import jakarta.validation.constraints.Size;
import lombok.AllArgsConstructor;
import lombok.Getter;
import lombok.NoArgsConstructor;
import lombok.Setter;

@Getter
@Setter
@NoArgsConstructor
@AllArgsConstructor
@Schema(description = "?щ（ ?뺣낫 ?섏젙 ?붿껌")
public class CrewUpdateRequest {

    @Size(min = 2, max = 50, message = "?щ（ ?대쫫? 2~50?먯뿬???⑸땲??")
    @Pattern(regexp = "^[a-zA-Z0-9媛-??\s]+$", message = "?щ（ ?대쫫? ?쒓?, ?곷Ц, ?レ옄, 怨듬갚留?媛?ν빀?덈떎.")
    @Schema(description = "?щ（ ?대쫫", example = "?쒖슱 ?щ떇 ?щ（")
    private String name;

    @Size(max = 500, message = "?щ（ ?뚭컻??500?먮? 珥덇낵?????놁뒿?덈떎.")
    @Schema(description = "?щ（ ?뚭컻", example = "?④퍡 ?щ━硫?嫄닿컯???쇱씠?꾩뒪??쇱쓣 異붽뎄?섎뒗 ?щ（?낅땲??)
    private String description;

    @Min(value = 2, message = "?щ（ ?뺤썝? 理쒖냼 2紐??댁긽?댁뼱???⑸땲??")
    @Max(value = 1000, message = "?щ（ ?뺤썝? 理쒕? 1000紐낃퉴吏 媛?ν빀?덈떎.")
    @Schema(description = "理쒕? ?몄썝", example = "20")
    private Integer maxMembers;

    @Pattern(regexp = "^https?://.*", message = "?щ컮瑜?URL ?뺤떇???꾨떃?덈떎.")
    @Schema(description = "?꾨줈???대?吏 URL (deprecated, profileImageKey ?ъ슜 沅뚯옣)", example = "https://example.com/crew-profile.jpg")
    private String profileImageUrl;

    @Schema(description = "?꾨줈???대?吏 S3 Key", example = "crews/123/profile_1234567890.jpg")
    private String profileImageKey;
}
