# 기본 프로파일 설정
spring:
  application:
    name: waytoearth-backend

  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

  config:
    import:
      - optional:dotenv:.env
      - optional:file:.env

  # Flyway 설정 (모든 환경 공통)
  flyway:
    enabled: true
    baseline-on-migrate: true
    baseline-version: 0
    locations: classpath:db/migration
    validate-on-migrate: true
    out-of-order: false

  # OAuth2 설정 (모든 환경 공통)
  security:
    oauth2:
      client:
        registration:
          kakao:
            client-id: ${KAKAO_CLIENT_ID}
            redirect-uri: ${KAKAO_REDIRECT_URI}
            authorization-grant-type: authorization_code
            client-authentication-method: POST
            scope:
              - profile_nickname
              - account_email
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: ${KAKAO_TOKEN_URI}
            user-info-uri: ${KAKAO_USER_INFO_URI}
            user-name-attribute: id

  # Redis 설정
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      timeout: 2000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
          max-wait: 2000ms

# 공통 설정
kakao:
  client-id: ${KAKAO_CLIENT_ID}
  redirect-uri: ${KAKAO_REDIRECT_URI}

# JWT 설정
jwt:
  secret: ${JWT_SECRET}
  access-token-expiration: ${JWT_ACCESS_EXPIRATION:900000}      # 액세스 토큰 (15분 = 900,000ms)
  refresh-token-expiration: ${JWT_REFRESH_EXPIRATION:2592000000} # 리프레시 토큰 (30일 = 2,592,000,000ms)

# AWS S3 설정 (기본값)
cloud:
  aws:
    region: ${AWS_REGION:ap-northeast-2}
    s3:
      bucket: ${S3_BUCKET:waytoearth-assets}  # 환경별로 오버라이드됨

# 날씨 API 설정
weather:
  openweather:
    api-key: ${OPENWEATHER_API_KEY:}

# OpenAI API 설정
openai:
  api-key: ${OPENAI_API_KEY}
  model: ${OPENAI_MODEL:gpt-3.5-turbo}
  max-tokens: ${OPENAI_MAX_TOKENS:800}
  temperature: ${OPENAI_TEMPERATURE:0.7}
  timeout-seconds: ${OPENAI_TIMEOUT:30}
  min-completed-records: ${OPENAI_MIN_RECORDS:5}
  analysis-history-limit: ${OPENAI_HISTORY_LIMIT:10}
  daily-limit-per-user: ${OPENAI_DAILY_LIMIT:10}

# Firebase Cloud Messaging (FCM) 설정
fcm:
  firebase:
    config-path: ${FCM_CONFIG_PATH:src/main/resources/firebase/waytoearth-firebase-adminsdk.json}
    config-json: ${FCM_CONFIG_JSON:}
    config-json-base64: ${FCM_CONFIG_JSON_BASE64:}
  notifications:
    enabled: ${FCM_ENABLED:true}
    scheduled:
      morning-time: "0 30 6 * * *"   # 매일 오전 6:30 (cron)
      evening-time: "0 0 20 * * *"   # 매일 오후 8:00 (cron)
      timezone: "Asia/Seoul"
      morning-message:
        title: "좋은 아침이에요! ☀️"
        body: "오늘도 상쾌한 러닝 어때요?"
      evening-message:
        title: "퇴근 후 러닝 타임! 🏃"
        body: "가벼운 러닝으로 하루를 마무리해보세요"

# 캐시 설정
cache:
  redis:
    time-to-live: 3600000 # 1시간 (밀리초)
    key-prefix: "waytoearth:"